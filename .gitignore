# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
# Только каталог dist в корне репозитория (wheel/sdist и т.п.)
/dist/
.python-version
backend/.venv/
venv/
.env
.env.*
!.env.example

# Node / Vite — любые node_modules (в т.ч. frontend/); локальный вывод Vite во frontend/dist/
node_modules/
frontend/dist/

# IDE / OS
.DS_Store
.idea/
.vscode/

# backend/static — продакшен-сборка Vite; собирается при deploy (npm run build).
# Не игнорируем: при желании можно коммитить артефакт; Railway пересобирает во frontend/.
