# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
# Только каталог dist в корне репозитория (wheel/sdist и т.п.)
/dist/
.python-version
backend/.venv/
venv/
# Локальная копия пака на сервере / после редактора (генерируется при сохранении)
backend/pack.json
.env
.env.*
!.env.example

# Node / Vite — любые node_modules (в т.ч. frontend/); локальный вывод Vite во frontend/dist/
node_modules/
frontend/dist/

# IDE / OS
.DS_Store
.idea/
.vscode/

# Продакшен-сборка Vite → backend/static (npm run build). Не коммитить: иначе на деплое
# могут оставаться старые index-*.css/js из git, если сборка не отработала.
backend/static/
